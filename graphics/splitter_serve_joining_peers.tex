\documentclass{article}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{algorithm}
\usepackage[noend]{algpseudocode}

\begin{document}
\pagestyle{empty}

\newcommand{\send}{\Rightarrow}
\newcommand{\sendto}{\rightarrow}
\newcommand{\recv}{\Leftarrow}
\algrenewcommand\algorithmicprocedure{\textbf{task}}

\begin{algorithmic}

  \Procedure{$S^t$.AddPeers}{}
  \While{True}
  \State $[\mathtt{hello}] \leftarrow P^t_i$
  %\If{$P^t_i \notin T^t$}
  \ForAll{$P^t_k \in T^t$}
  \State $[P^t_k] \send P^t_i$
  \EndFor
  \State $T^t = T^t \cup P^t_i$
  %\Else~\texttt{/* received $[\mathtt{goodbye}]$ */}
  %\State $[\mathtt{goodbye}] \sendto P^_i$
  %\State ${\cal T}_j={\cal T}_j \setminus {\cal P}_i$
  %\EndIf
  \State $[|{\cal T}_j|] \send {\cal R}$
  \EndWhile
  \EndProcedure
  
\end{algorithmic}

\end{document}


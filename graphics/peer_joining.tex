\documentclass{article}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{algorithm}
\usepackage[noend]{algpseudocode}

\begin{document}
\pagestyle{empty}

\newcommand{\send}{\Rightarrow}
\newcommand{\sendto}{\rightarrow}
\algrenewcommand\algorithmicprocedure{\textbf{task}}
\algrenewcommand\textproc{\textrm}

\begin{algorithmic}
  \Procedure{Peer.join\_a\_team}{Splitter}
  \State $[\mathtt{hello}] \send$ Splitter
  \ForAll{$P_k\in[\mathrm{list~of~peers}]\gets$ Splitter}
  \State $[\mathtt{hello}]\sendto P_k$
  \EndFor
  \EndProcedure
\end{algorithmic}
\begin{algorithmic}
  %\newline
  \Procedure{$P_k$.AddNeighbors}{}
  \While{True}
  \State $[\mathtt{hello}] \gets P_i$
  %\State $[\mathtt{hello}] \sendto {\cal P}_i$
  %\State $N({\cal P}_k) = N({\cal P}_k) \cup {\cal P}_i$
  \State $T^i = T^i \cup P_i$
  \State $\mathtt{debt}[P_i]=0$
  \State \textbf{sort} $T^i$ \textbf{ascending by} $\mathtt{debt[]}$
  \EndWhile
  \EndProcedure
  
\end{algorithmic}

\end{document}


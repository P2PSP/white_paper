\documentclass{article}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{algorithm}
\usepackage[noend]{algpseudocode}

\begin{document}
\pagestyle{empty}

\newcommand{\send}{\Rightarrow}
\newcommand{\sendto}{\rightarrow}
\algrenewcommand\algorithmicprocedure{\textbf{task}}

\begin{algorithmic}

  \Procedure{$P^t_o$.Leave}{$T^t$}
  \Repeat
  \State $[\mathtt{goodbye}] \sendto S^t$
  \State $[\mathtt{goodbye}] \gets S^t$ \textbf{with} timeout
  \Until{\textbf{not} timeout}
  \State relay\_pending\_chunks()
  \State $[\mathtt{goodbye}] \sendto S^t$
  \ForAll{$P^t_k\in T^t_o$}
  \State $[\mathtt{goodbye}] \sendto P^j_k$
  \EndFor
  \EndProcedure
  \newline
  \Procedure{$P^t_k$.RemoveNeighbors}{}
  \While{True}
  \State $[\mathtt{goodbye}] \gets P^t_o$
  \State $T^t_k = T^t_k \setminus P^ t_o$
  \EndWhile
  \EndProcedure
  
\end{algorithmic}

\end{document}

